
(async () => {
    const profileData = await getProfileData();
    

    function convertProfileData (getProfileData){

        const name = profileData.name;
        const job = profileData.job;
        const location = profileData.location;
        const phone = profileData.phone;
    }

})()